TODO

 * parameter to show other fields than email in autocompletion list (Name, Address, ...)

DONE

 * enable/disable Completion for SelfService
 * add completion against LDAP
 * optimisation to match privileged/unprivileged criteria (faster than the if ... elsif ... else inside the loop)